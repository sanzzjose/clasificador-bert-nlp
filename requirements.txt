# Entorno reproducible del proyecto (Python 3.9, CPU).
# Se instalo en un entorno virtual aislado (.venv) porque el entorno global tenia
# conflictos de versiones (NumPy 2 vs TensorFlow, mezcla de versiones de TF, etc.).
#
# Nota importante: aiohttp se fija por debajo de 3.12 porque las versiones nuevas usan una
# anotacion de tipos que rompe en Python 3.9.0 concretamente (bug del modulo typing).
tensorflow==2.15.1
numpy==1.26.4
transformers==4.57.6
datasets==4.5.0
aiohttp<3.12
scikit-learn==1.6.1
pandas==2.3.3
matplotlib==3.9.4
seaborn==0.13.2
nbconvert==7.17.1
ipykernel==6.31.0
